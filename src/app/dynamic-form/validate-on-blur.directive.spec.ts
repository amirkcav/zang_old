import { ValidateOnBlurDirective } from './validate-on-blur.directive';

describe('ValidateOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateOnBlurDirective(null);
    expect(directive).toBeTruthy();
  });
});
